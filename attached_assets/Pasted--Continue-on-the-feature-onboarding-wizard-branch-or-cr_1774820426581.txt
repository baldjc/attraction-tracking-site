  Continue on the "feature/onboarding-wizard" branch (or create a new branch "feature/payment-reminder-sms" if the onboarding branch has already
  been merged).

  We're adding a "Send Payment Reminder" feature for past-due subscriptions. An admin clicks a button, the system grabs the Stripe payment link,
  and texts it to the member via GHL.

  === CHANGE 1: ADD SMS FUNCTION TO GHL UTILITY ===

  Modify: src/lib/ghl.ts

  Add a new function to send an SMS through GHL:

  export async function sendSmsToContact(contactId: string, message: string): Promise<boolean> {
    try {
      const res = await fetch(
        `${GHL_BASE_URL}/conversations/messages`,
        {
          method: "POST",
          headers: {
            Authorization: `Bearer ${getApiKey()}`,
            Version: "2021-07-28",
            "Content-Type": "application/json",
          },
          body: JSON.stringify({
            type: "SMS",
            contactId,
            message,
          }),
        }
      );
      return res.ok;
    } catch {
      return false;
    }
  }

  === CHANGE 2: STRIPE INVOICE RETRIEVAL ===

  Modify: src/lib/stripe.ts

  Add a function to get the latest unpaid invoice's hosted payment URL:

  import Stripe from "stripe";

  // Ensure stripe client is exported (may already be)
  export const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);

  export async function getPaymentRetryUrl(stripeCustomerId: string): Promise<string | null> {
    try {
      const invoices = await stripe.invoices.list({
        customer: stripeCustomerId,
        status: "open",
        limit: 1,
      });
      const invoice = invoices.data[0];
      if (!invoice) return null;
      return invoice.hosted_invoice_url ?? null;
    } catch {
      return null;
    }
  }

  === CHANGE 3: PAYMENT REMINDER API ROUTE ===

  Create: src/app/api/admin/members/[id]/send-payment-reminder/route.ts

  POST handler:
  1. Auth check — must be admin role (use resolveUserFromSession, check role)
  2. Fetch the member from the database: id, fullName, phone, email, stripeCustomerId, subscriptionStatus
  3. Validate:
     - Member must have a stripeCustomerId (return 400 "No Stripe customer linked")
     - Member must have a phone number (return 400 "No phone number on file")
     - Member's subscriptionStatus should be "past_due" or "unpaid" (return 400 "Subscription is not past due" if status is "active" or "canceled"
  — but allow override with ?force=true query param for testing)
  4. Call getPaymentRetryUrl(stripeCustomerId) — if null, return 400 "No open invoice found"
  5. Look up the GHL contact: fetchContactByEmail(member.email)
     - If no GHL contact found, return 400 "Member not found in GHL"
  6. Build the SMS message:
     "Hi {firstName}, your Attraction by Video subscription payment didn't go through. You can update your payment method and retry here:
  {paymentUrl} — Jared, Attraction by Video"
     (Use the first word of fullName as firstName)
  7. Call sendSmsToContact(ghlContact.id, message)
  8. If successful, return { success: true, message: "Payment reminder sent via SMS" }
  9. If SMS fails, return 500 "Failed to send SMS"

  === CHANGE 4: ADMIN MEMBER DETAIL — SEND REMINDER BUTTON ===

  Modify the admin member detail page (wherever subscription status is displayed).

  When subscriptionStatus is "past_due" or "unpaid", show a button:
  - Label: "Send Payment Reminder"
  - Style: bg-amber-500 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-amber-600
  - Icon: phone or message icon from Heroicons
  - On click: POST to /api/admin/members/{id}/send-payment-reminder
  - While sending: show "Sending..." disabled state
  - On success: show green toast/flash "Payment reminder sent to {phone}"
  - On error: show red toast/flash with the error message

  Also show the member's phone number near this button so the admin can visually verify it before sending.

  === CHANGE 5: MEMBER-FACING PAYMENT BANNER ===

  When a member with subscriptionStatus "past_due" or "unpaid" logs in, show a banner at the top of the dashboard (above the OnboardingBanner):

  - Style: bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-700/40 rounded-lg p-4
  - Icon: exclamation triangle
  - Text: "Your subscription payment didn't go through. Please update your payment method to keep your access."
  - Button: "Update Payment" — clicking this calls a new API route GET /api/member/payment-retry-url that returns the hosted_invoice_url for the
  current member's Stripe customer. Opens the URL in a new tab.

  Create: src/app/api/member/payment-retry-url/route.ts
  - Auth via resolveUserFromSession
  - Fetch user's stripeCustomerId from database
  - Call getPaymentRetryUrl(stripeCustomerId)
  - Return { url: string | null }

  === CHANGE 6: TEST IT ===

  IMPORTANT: Before this goes live, we need to test with Jared Chamberlain's account.

  After building everything:
  1. Find Jared Chamberlain's member record in the database
  2. Log his current phone number and subscriptionStatus to the console
  3. Verify the phone number matches what's in GHL by calling fetchContactByEmail with his email
  4. Log the GHL contact's phone to confirm they match
  5. DO NOT actually send an SMS yet — just log what WOULD be sent:
     - Log: "TEST MODE — Would send SMS to: {phone}"
     - Log: "TEST MODE — Message: {message}"
     - Log: "TEST MODE — Payment URL: {url}"

  Add a DRY_RUN mode: if the query param ?dryRun=true is passed to the send-payment-reminder endpoint, log everything but don't actually call
  sendSmsToContact. Return { success: true, dryRun: true, phone, message, paymentUrl } so we can verify everything looks right in the response.

  === VERIFICATION ===

  1. npm run build — should succeed
  2. On the admin member detail page for a member with past_due status, the "Send Payment Reminder" button should appear
  3. Clicking with ?dryRun=true should return the message preview without sending
  4. The member-facing payment banner should appear for past_due members
  5. The "Update Payment" button should open the Stripe hosted invoice page

  The ?dryRun=true flag lets you verify the phone number, message text, and payment URL all look correct before actually sending the SMS. Once you
  confirm it looks right, you can hit it without the flag to send it for real.