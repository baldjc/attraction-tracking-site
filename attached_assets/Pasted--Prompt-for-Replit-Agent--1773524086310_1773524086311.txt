  Prompt for Replit Agent:
                                                                                                                                         
  Two things to build:

  ## 1. Fix GHL Sync — Only Pulling 1 Contact

  The GHL sync in src/lib/ghl.ts has broken pagination. The GET /contacts/ endpoint uses cursor-based pagination, not numeric offsets.

  Fix the fetchContactsByTag function:
  - First request: GET /contacts/?locationId={GHL_LOCATION_ID}&limit=100 (no startAfter param)
  - Look at the LAST contact in each response page and use its ID for the next request: startAfterId={lastContact.id}
  - Keep paginating until data.contacts.length < limit
  - Add console.log showing: total contacts fetched, how many matched the tag, how many pages retrieved

  Alternatively, try the search endpoint which supports tag filtering natively:
  - POST https://services.leadconnectorhq.com/contacts/search
  - Headers: Authorization: Bearer {GHL_API_KEY}, Version: 2021-07-28, Content-Type: application/json
  - Body: { "locationId": "{GHL_LOCATION_ID}", "pageSize": 100, "filters": [{ "field": "tags", "operator": "contains", "value":
  "foundations - weekly coaching" }] }

  Use whichever approach successfully returns all 22 tagged contacts.

  ## 2. Build Member Detail Page (/admin/members/[id])

  When an admin clicks a member's name in the Members list, it should open a rich detail page designed to help a YouTube coach manage
  that member. Layout from top to bottom:

  **HEADER SECTION:**
  - YouTube channel banner image (full width, pulled from YouTube channel URL if available — for now use a gradient placeholder if no
  banner)
  - Overlay on the banner: member's name (large), YouTube handle, service tier badge, and a "View Channel" button linking to their
  YouTube URL
  - Below the banner: key stats row — Current Score (colour-coded), Member Since date, Last Audit date, Total Videos Analysed

  **MEMBER INFO CARD:**
  - Email, GHL Contact ID (linked to GHL if possible), YouTube Channel URL
  - Edit button to update any of these fields
  - Service tier dropdown (Foundations, Editing 2, Editing 4, Scaling 2, Scaling 4) — editable inline

  **AUDIT HISTORY SECTION:**
  - Table: Date | Audit Type | Overall Score (colour-coded) | Action (View Report link)
  - Score colour coding: 7.0+ green, 5.0–6.9 yellow, below 5.0 red
  - "Run Audit" dropdown button with options: Baseline, Monthly, Single Video (these can be placeholder actions for now — we'll wire them
   up in Phase 4)
  - If no audits yet, show: "No audits yet — use the Run Audit button to generate the first baseline."

  **SCORE TREND SECTION:**
  - Line chart (use Recharts) showing overall score over time across all audits
  - If only one audit, show it as a single point with a note: "More data points will appear after monthly audits"
  - If no audits, show empty state

  **16-PRINCIPLE BREAKDOWN:**
  - Pull from the most recent audit's scores JSON
  - Table: Principle Name | Current Score (colour-coded) | Δ from Baseline
  - Group into 4 dimensions:
    - 🎯 Channel Strategy: Avatar Clarity, Themes Over Topics, Consistency
    - 🎬 Content Impact: ARC Attention, ARC Revelation, Approve the Click, Title Frameworks, Show Don't Tell, Curiosity Bridges
    - 🤝 Viewer Connection: Connection Language, Values Peppering, Story Proof
    - 📈 Lead Generation: Lead Magnet System, Binge Architecture
  - Show dimension averages as section headers
  - If no audits, show placeholder: "Scores will appear after the first audit"

  **TRACKING LINKS SECTION:**
  - Table of this member's tracking links: Link Name | Short URL | Clicks | Conversions | Conv. Rate
  - "Create Link for Member" button (placeholder for now)
  - If no links: "No tracking links yet"

  **COACHING NOTES SECTION:**
  - Simple text area where admin can write and save private notes about this member
  - Add a "coaching_notes" text field to the users table in Prisma if it doesn't exist
  - Save on blur or with a Save button
  - Show last updated timestamp

  **QUICK ACTIONS SIDEBAR (or top-right area):**
  - Run Audit (dropdown: Baseline, Monthly, Single Video)
  - View on YouTube (external link)
  - Email Member (mailto: link)
  - View in GHL (link to GHL contact if ghlContactId exists — URL format:
  https://app.gohighlevel.com/v2/location/{locationId}/contacts/detail/{contactId})

  Use the app's existing colour palette (#f1f1ef backgrounds, #3dc3ff primary, #ff0033 alerts, #1e2a38 text). Make it mobile-responsive.
  This page should feel like a coaching dashboard for one member.