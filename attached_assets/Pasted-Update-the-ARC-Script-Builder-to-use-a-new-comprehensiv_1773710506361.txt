Update the ARC Script Builder to use a
   new comprehensive prompt system.     
  Reference                             
  docs/ARC-Script-Builder-Prompt.md for 
  the full prompt content.              
                                      
  Here's what needs to change:        

  1. src/app/api/ai-tools/arc-script-bui
  lder/route.ts

  - Create a shared masterPrompt string
  containing the Master System Prompt
  from
  docs/ARC-Script-Builder-Prompt.md.
  Replace {{MEMBER_CONTEXT}} with the
  existing contextBlock variable.
  - Send masterPrompt as the system
  parameter in every
  client.messages.create() call. The
  step-specific prompt goes in messages
  as the user message.
  - Replace all 4 step prompts (opening,
   credibility, insights, final) with
  the Step-Specific Prompts from the
  doc.
  - The opening step must now also
  return expertise_bridges (3 options:
  Authority Bridge, Revelation Bridge,
  Pattern Bridge) — see the doc for the
  exact JSON shape.
  - The final step must now also return
  retention_suggestions (array of
  {location, issue, fix}) — see the doc
  for the exact JSON shape.
  - Set max_tokens: 4096 for both the
  opening and final steps, 2048 for
  credibility and insights.

  2. src/components/ai-tools/ArcScriptBu
  ilderTool.tsx

  - Add new state: expertiseBridges
  (array), selectedBridge (string), and
  retentionSuggestions (array).
  - In goToStep4(), also capture
  result.expertise_bridges from the API
  response.
  - In Step 4 UI (opening), add an
  "Expertise Bridge" picker section
  BELOW the intro pattern picker. Show 3
   selectable cards — one for each
  bridge type. Each card shows the
  bridge name, the generated script
  text, and a best_when hint in smaller
  text. User must select one before
  proceeding.
  - Pass selectedBridge and
  leadMagnetLine through to the final
  step's allStepData.
  - In Step 5 UI (final output), add a
  "Retention Analysis" section that
  renders each suggestion with its
  location, issue, and fix.
  - Add
  expertise_bridge_after_lead_magnet to
  the checklist interface and
  CHECKLIST_LABELS map with label
  "Expertise bridge comes after lead
  magnet".
  - The contradiction intro patterns are
   already grouped by sub-type with 2
  variations each (that code change is
  already in the file).

  No database changes needed. No new
  routes. No new pages.