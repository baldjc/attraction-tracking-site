 Copy this into your Replit agent:

  ▎ We migrated from Prisma Postgres
  (local) to Neon, but the site is
  broken — I can't log in. Here's what
   needs to be fixed:

  ▎ 1. Fix the DATABASE_URL in the
  Replit Secrets
  ▎ The .env file still has the old pr
  isma+postgres://localhost:51213/... 
  URL. Update the DATABASE_URL secret
  to the Neon connection string. It
  should look like:
  ▎ postgresql://neondb_owner:PASSWORD
  @ep-XXXX.us-east-2.aws.neon.tech/neo
  ndb?sslmode=require
  ▎ Use the Neon pooled connection
  string (the one ending in -pooler).
  Check the Neon dashboard for the
  correct URL.

  ▎ 2. Verify prisma.ts is using
  @prisma/adapter-pg correctly
  ▎ The file src/lib/prisma.ts uses
  PrismaPg adapter with
  process.env.DATABASE_URL. This
  should work fine with a standard
  Neon PostgreSQL URL — just make sure
   the DATABASE_URL secret is set
  correctly (step 1).

  ▎ 3. Run the Prisma migration
  against Neon
  ▎ The Neon database likely has no
  tables yet. Run:
  ▎ npx prisma db push
  ▎ This will create all the tables in
   Neon based on prisma/schema.prisma.

  ▎ 4. Seed my admin user
  ▎ After the tables exist, I need my
  admin account created. Run this in a
   Node script or Prisma seed:
  ▎ import bcrypt from "bcryptjs";
  ▎ const hash = await 
  bcrypt.hash("PICK_A_TEMP_PASSWORD",
  12);
  ▎ await prisma.user.create({
  ▎   data: {
  ▎     email:
  "jared@chamberlaingroup.ca",
  ▎     fullName: "Jared Chamberlain",
  ▎     passwordHash: hash,
  ▎     role: "admin",
  ▎     emailVerified: true,
  ▎   }
  ▎ });

  ▎ 5. Make sure these env vars are
  set in Replit Secrets:
  ▎ - DATABASE_URL — Neon pooled
  connection string
  ▎ - NEXTAUTH_SECRET — any random
  string (generate with openssl rand
  -base64 32)
  ▎ - NEXTAUTH_URL — the live Replit
  URL (e.g., https://attraction-tracki
  ng-site.replit.app)

  ▎ 6. Rebuild and restart
  ▎ npx prisma generate && npm run
  build && npm run start

  ▎ The login is password-based
  (NextAuth Credentials provider in
  src/lib/auth.ts), NOT magic links.
  If the DB connection fails, login
  silently returns "Invalid email or
  password."