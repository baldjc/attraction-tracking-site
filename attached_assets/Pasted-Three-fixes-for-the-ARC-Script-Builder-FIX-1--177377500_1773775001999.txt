Three fixes for the ARC Script Builder:                   
                           
  === FIX 1: OPENING PATTERNS — MERGE INTO ONE SECTION +    
  MORE HOOKS ===                                            
                                                            
  The "Other Opening Patterns" (Confirmation, Empathy,      
  Stakes) look like a separate disconnected section. They
  need to live INSIDE the same card as the Contradiction    
  openings — one unified list of all opening options the
  member can pick from.                          

  Also, the hook ideas section needs improvement:           
  1. Generate 5-7 hook starter ideas (not just 2-3)
  2. Add a "Generate More Hooks" button that calls the API  
  for another batch                                         
  3. Keep the hooks inside the same opening card — not as   
  their own separate section                                
                                                 
  Everything related to openings (all patterns + hooks +    
  lead magnet line) should be ONE card.
                                                            
  === FIX 2: NEXT VIDEO PUSH — ADD "CHOOSE FROM MY VIDEOS"  
  BUTTON ===                                     
                                                            
  The Next Video Push section currently only has manual text
   inputs. Add a second option:                  
                                                            
  1. Add a "Choose from my videos" button next to the title 
  input                                          
  2. When clicked, fetch the member's YouTube videos from   
  the existing YouTube API integration (the same one used   
  for audits)                                    
  3. Show a searchable/scrollable list of their video titles
   — member picks one                                       
  4. When a video is selected, auto-fill the title field AND
   fetch the first 30 seconds of that video's transcript    
  (using the YouTube transcript API)
  5. The AI then uses that transcript excerpt to write a    
  more specific, natural bridge and closing CTA — because it
   knows what the next video actually says in its opening
                                                            
  Keep the manual input option too — some members may want  
  to push to a video they haven't uploaded yet. So the UI
  should be:                                                
  - Text input for title (manual entry) OR "Choose from my
  videos" button                                            
  - If chosen from videos, show the selected video title
  with an X to clear and the transcript excerpt in a subtle 
  read-only box below
  - "Why should they watch it?" textarea remains for both   
  paths (but if transcript is available, AI can draft this  
  too)                                           
                                                            
  === FIX 3: CREDIBILITY INPUT NOT REACHING THE FINAL SCRIPT
   ===                                           
                                                            
  The member entered "22 years in the industry" and selected
   a credibility section, but the final script says "8 years
   in Calgary real estate" — completely ignoring the        
  member's input. 
                                                 
  This is a prompt/data flow bug. Debug and fix:            
  
  1. Check that the credentialInput from Step 2             
  (Credibility) is actually being passed through to the
  "final" step API call in allStepData                      
  2. Check that the "final" prompt template includes the
  credentialInput value — not just the avatar's default     
  credentials
  3. The member's explicit credibility input should OVERRIDE
   any avatar-stored credentials for this script            
  4. Add explicit instruction in the final prompt:
  "CRITICAL: Use the member's credibility input EXACTLY as  
  provided. Do not substitute with avatar defaults. The
  member said: [credentialInput]"                           
  5. Also audit: are the selected opening, selected
  credibility suggestions, member's values, and member's    
  interests ALL actually being included in the final API
  call? Log allStepData to the console before the final API 
  call so we can verify nothing is being dropped.
                                                 
  The final script must reflect EVERY choice the member made
   in the previous steps. If a member selects something, it
  needs to appear in the output.                            
                  
  === HOW TO TEST ===                                       
  - [ ] All opening patterns (Contradiction variants +
  Confirmation + Empathy + Stakes) appear in ONE card       
  - [ ] 5-7 hook ideas generated, "Generate More" button
  works                                                     
  - [ ] "Choose from my videos" button appears in Next Video
   Push                                                     
  - [ ] Clicking it shows a list of the member's YouTube
  videos                                                    
  - [ ] Selecting a video auto-fills the title and fetches
  transcript                                                
  - [ ] AI uses the transcript to write a better bridge/CTA
  - [ ] Manual entry still works as fallback                
  - [ ] Credibility input from the member appears verbatim  
  in the final script (not overridden by avatar defaults)   
  - [ ] Console log confirms all step data is present in the
   final API call