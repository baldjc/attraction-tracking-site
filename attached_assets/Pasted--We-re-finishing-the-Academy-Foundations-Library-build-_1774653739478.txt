```
We're finishing the Academy / Foundations Library build on the "feature/academy-foundations-library" branch. Make sure you're on that branch. This is Part 4 of 4 — audit report integration and polish.

=== CHANGE 1: AUDIT REPORT PRINCIPLE LINKS ===

Update the existing audit report components (wherever principle scores are displayed to members) to include a link to related Academy lessons.

When displaying a principle score (e.g., "Avatar Clarity: 4/10"), add a link underneath or beside it:
- Text: "See lessons →"
- Link: /member/academy/principles?tag=[principle_slug]
- Only show the link if at least one lesson is tagged with that principle
- Style: subtle, secondary link (not a big button — should feel helpful, not pushy)

The principle slugs match between the audit system and the Academy:
avatar_clarity, themes_over_topics, lead_magnet, arc_attention, arc_revelation, arc_connection, connection_language, approve_the_click, title_frameworks, curiosity_bridges, binge_architecture, consistency, values_peppering, grade_5_language, story_proof, show_dont_tell, studio_setup


=== CHANGE 2: MEMBER DASHBOARD CARD ===

Add an "Academy" card to the member dashboard (the main /member page):
- Card title: "The Foundations Library"
- Shows overall progress: "X/22 lessons complete" with progress bar
- "Continue Learning" button → links to first incomplete lesson
- If 100% complete, show a congratulations message instead
- Style: consistent with existing dashboard cards


=== CHANGE 3: POLISH & TESTING ===

Review all Academy pages for:

1. Mobile responsiveness — all pages must work well on mobile
2. Loading states — show skeleton loaders or spinners while data loads
3. Empty states — handle cases where no progress exists yet gracefully
4. Error handling — auto-save failures should show a clear retry indicator, not break the page
5. YouTube embeds — must be responsive (16:9 aspect ratio, full width on mobile)
6. Tab/section navigation on lesson page should be smooth (no page jumps)
7. Workbook auto-save debounce should be 1 second after last keystroke
8. Mark as Complete should give visual feedback (checkmark animation or colour change)
9. Browse by Principle page should work with ?tag= query param (auto-filter on load)
10. All principle tag badges should have consistent colours across all pages

Run through these test scenarios:
- Navigate: Dashboard → Academy → Foundations → Section 1 → Lesson → fill in workbook fields → mark complete → next lesson
- Check that progress bars update correctly as lessons are marked complete
- Test workbook auto-save: type in a field, wait 1s, refresh page — response should persist
- Test homework checkboxes: check items, refresh — state should persist
- Admin: go to Course Manager → edit a lesson → add a workbook field → verify it appears on member side
- Test the audit report principle links → click through to see filtered lessons

IMPORTANT: Commit your work on the feature/academy-foundations-library branch. Do NOT merge to main. Do not create a PR. Just leave it on the branch for review.
```